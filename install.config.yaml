- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.zshrc: zshrc
    ~/.zprofile: zprofile

- shell:
  - [git submodule update --init --recursive, Installing submodules]

