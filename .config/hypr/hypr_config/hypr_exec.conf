#-- Execute -------------------------------------------------
# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox

# kanshi works, but to do screensharing an entire screen, I need to put the monitors bitdepth to 10, there is no options in hanshi to do that

# exec = kanshi
source = ~/.config/hypr/hypr_config/hypr_scripts.conf

exec-once = /usr/lib/polkit-kde-authentication-agent-1
exec-once = mako
exec-once = $lockscreentime
# exec-once = hypridle