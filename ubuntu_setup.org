#+title: Ubuntu Setup
#+startup: overview
#+PROPERTY: header-args:shell :tangle ~/.dotfiles/.ubuntu_setup.sh :mkdirp yes
#+INCLUDE: "~/.dotfiles/system_info.org"


* Update
#+begin_src shell
# a comment
sudo apt update && sudo apt upgrade
snap refresh
#+end_src
* Install git
** Install
#+begin_src shell
sudo apt install git-all
#+end_src
** Configure
#+begin_src shell
git config --global user.name "Keisn1"
git config --global user.email kay.freyer@icloud.com
#+end_src
* Download Repo
#+begin_src shell
git clone https://github.com/Keisn1/.dotfiles.git
cd ~/.dotfiles
git checkout ubuntu_asus
git pull origin ubuntu_asus
cd
#+end_src
* Codecs for audio, video playback, Microsoft fonts
#+begin_src shell
sudo apt install ubuntu-restricted-extras
#+end_src
* Install Fonts
#+begin_src shell
curl -OL https://github.com/ryanoasis/nerd-fonts/releases/download/v3.0.2/JetBrainsMono.zip
# FONT_VERSION=$(curl -s "https://api.github.com/repos/JetBrains/JetBrainsMono/releases/latest" | grep -Po '"tag_name": "v\K[0-9.]+')
# curl -sSLo jetbrains-mono.zip https://download.jetbrains.com/fonts/JetBrainsMono-$FONT_VERSION.zip
# unzip -qq jetbrains-mono.zip -d jetbrains-mono
unzip -qq JetBrainsMono.zip -d jetbrains-mono-nerd
sudo mkdir ~/.local/share/fonts/
sudo mv jetbrains-mono-nerd ~/.local/share/fonts
rm -r jetbrains-mono-nerd
rm JetBrainsMono.zip
#+end_src
* Gnome Tweaks and dconf
#+begin_src shell
sudo apt install gnome-tweaks dconf-editor
#+end_src
* Gnome Settings
#+begin_src shell :noweb yes
dconf load / < ~/.dotfiles/gnome/dconf-settings-<<system_info.org:HOSTNAME()>>-<<system_info.org:OS()>>.ini
#+end_src
* Load profile with dconf
#+begin_src shell
dconf load /org/gnome/terminal/legacy/profiles:/:b1dcc9dd-5262-4d8d-a863-c897e6d979b9/ < ~/.dotfiles/terminal/profiles/ubuntu1.dconf
#+end_src
* Install Firefox
#+begin_src shell
sudo snap install firefox
#+end_src
* Install Doom Emacs
#+begin_src shell
sudo apt install emacs          # no email configuration
git clone --depth 1 https://github.com/doomemacs/doomemacs ~/.config/emacs
PATH=$PATH:~/.config/emacs/bin
doom install
#+end_src
* Oh-my-zsh
#+begin_src shell
sudo apt install git-core zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
#+end_src
** Fonts powerline
#+begin_src shell
sudo apt install fonts-powerline
#+end_src
** Starship
#+begin_src shell
curl -sS https://starship.rs/install.sh | sh
#+end_src
** zsh-syntax-highlighting
#+begin_src shell
git clone https://github.com/zsh-users/zsh-syntax-highlighting ~/.oh-my-zsh/custom/plugins/zsh-syntax-highlighting
#+end_src
** zsh-autosuggestions
#+begin_src shell
git clone https://github.com/zsh-users/zsh-autosuggestions ~/.oh-my-zsh/custom/plugins/zsh-autosuggestions
#+end_src
** zsh-vi-mode
#+begin_src shell
git clone https://github.com/jeffreytse/zsh-vi-mode $ZSH_CUSTOM/plugins/zsh-vi-mode
#+end_src
* Setup ssh
** Generate key
#+begin_src shell :noweb yes
ssh-keygen -t ed25519 -C "kay.freyer@icloud.com"
# - ssh-keygen -t ed25519 -C "kay.freyer@icloud.com"
#   - save under /home/kaypro/.ssh/<<system_info.org:HOSTNAME()>>-<<system_info.org:OS()>>_rsa
#   - add passphrase to lastPass {username}{PC}{OS}
#+end_src
- ssh-keygen -t ed25519 -C "kay.freyer@icloud.com"
  - save under .ssh/{username}{PC}{OS}_rsa
  - add passphrase to lastPass {username}{PC}{OS}
** Adding ssh_key to the ssh-agent
#+begin_src shell :noweb yes
sudo apt-get install keychain
eval $(keychain --eval ~/.ssh/<<system_info.org:HOSTNAME()>>-<<system_info.org:OS()>>_rsa)
# eval "$(ssh-agent -s)"
# ssh-add ~/.ssh/KayProAsusUbuntu_rsa
#+end_src
** Add pubic key to github
- cat ~/.ssh/id_ed25519.pub
- add ssh key on github
* Install stow
#+begin_src shell
sudo apt-get -y install stow
stow .
#+end_src
* Doom sync
#+begin_src shell
doom sync
#+end_src
* Utilities
** Ripgrep
#+begin_src shell
sudo apt install ripgrep
#+end_src
** Markdown
#+begin_src shell
sudo apt-get install markdown
#+end_src
** wl-clipboard
#+begin_src shell
sudo apt-get install wl-clipboard
#+end_src
** gnome-screenshot
#+begin_src shell
sudo apt-get install gnome-screenshot
#+end_src
** Graphviz
#+begin_src shell
sudo apt-get install graphviz
#+end_src
** shfmt
#+begin_src shell
sudo snap install shfmt
#+end_src
** ShellCheck
#+begin_src shell
sudo apt install shellcheck
#+end_src
** fd-find
#+begin_src shell
sudo apt install fd-find
#+end_src
* C compiler
#+begin_src shell
sudo apt install gcc g++
#+end_src
* JavaScript
** Node.js
#+begin_src shell
sudo apt install nodejs
#+end_src
** npm
#+begin_src shell
sudo apt install npm
#+end_src
* Python
** Pyenv
#+begin_src shell
git clone https://github.com/pyenv/pyenv.git ~/.pyenv
pyenv install $(pyenv install --list | grep -v - | grep -v b | grep 3.11 | tail -n 1)
pyenv global $(pyenv install --list | grep -v - | grep -v b | grep 3.11 | tail -n 1)
#+end_src
** Python/pyenv requirements
#+begin_src shell
sudo apt install \
    build-essential \
    curl \
    libbz2-dev \
    libffi-dev \
    liblzma-dev \
    libncursesw5-dev \
    libreadline-dev \
    libsqlite3-dev \
    libssl-dev \
    libxml2-dev \
    libxmlsec1-dev \
    llvm \
    make \
    tk-dev \
    wget \
    xz-utils \
    zlib1g-dev
#+end_src
** Install Dev Requirements by default
#+begin_src shell
pip install ~/.dotfiles/python/requirements-dev.txt
#+end_src
* Installing qutebrowser
** Installing via virtualenv
#+begin_src shell

#+end_src
