#+title: Ubuntu Setup
#+startup: overview
#+PROPERTY: header-args:shell :tangle ~/.dotfiles/.ubuntu_setup.sh :mkdirp yes
#+INCLUDE: "~/.dotfiles/system_info.org"

* Update
#+begin_src shell
# a comment
sudo apt update && sudo apt upgrade
snap refresh
#+end_src

* Setup ssh
** Generate key
#+begin_src shell :noweb yes
ssh-keygen -t ed25519 -C "kay.freyer@icloud.com"
# - ssh-keygen -t ed25519 -C "kay.freyer@icloud.com"
#   - save under /home/kaypro/.ssh/<<system_info.org:HOSTNAME()>>-<<system_info.org:OS()>>_rsa
#   - add passphrase to lastPass {username}{PC}{OS}
#+end_src
- ssh-keygen -t ed25519 -C "kay.freyer@icloud.com"
  - save under .ssh/{username}{PC}{OS}_rsa
  - add passphrase to lastPass {username}{PC}{OS}
** Adding ssh_key to the ssh-agent
#+begin_src shell :noweb yes
sudo apt-get install keychain
eval $(keychain --eval $HOME/.ssh/<<system_info.org:HOSTNAME()>>-<<system_info.org:OS()>>_rsa)
# eval "$(ssh-agent -s)"
# ssh-add $HOME/.ssh/KayProAsusUbuntu_rsa
#+end_src
** Add pubic key to github
- cat $HOME/.ssh/id_ed25519.pub
- add ssh key on github
* Install git
** Install
#+begin_src shell
sudo apt install git-all
#+end_src
** Configure
#+begin_src shell
git config --global user.name "Keisn1"
git config --global user.email kay.freyer@icloud.com
#+end_src
* Download Repo
#+begin_src shell
git clone https://github.com/Keisn1/.dotfiles.git
cd $HOME/.dotfiles
git checkout ubuntu_asus
git pull origin ubuntu_asus
cd
#+end_src
* Codecs for audio, video playback, Microsoft fonts
#+begin_src shell
sudo apt install ubuntu-restricted-extras
#+end_src
* Install Fonts
#+begin_src shell
curl -OL https://github.com/ryanoasis/nerd-fonts/releases/download/v3.0.2/JetBrainsMono.zip
# FONT_VERSION=$(curl -s "https://api.github.com/repos/JetBrains/JetBrainsMono/releases/latest" | grep -Po '"tag_name": "v\K[0-9.]+')
# curl -sSLo jetbrains-mono.zip https://download.jetbrains.com/fonts/JetBrainsMono-$FONT_VERSION.zip
# unzip -qq jetbrains-mono.zip -d jetbrains-mono
unzip -qq JetBrainsMono.zip -d jetbrains-mono-nerd
sudo mkdir $HOME/.local/share/fonts/
sudo mv jetbrains-mono-nerd $HOME/.local/share/fonts
rm -r jetbrains-mono-nerd
rm JetBrainsMono.zip
#+end_src
* Gnome Tweaks and dconf
#+begin_src shell
sudo apt install gnome-tweaks dconf-editor
#+end_src
* Gnome Settings
#+begin_src shell :noweb yes
dconf load / < $HOME/.dotfiles/gnome/dconf-settings-<<system_info.org:HOSTNAME()>>-<<system_info.org:OS()>>.ini
#+end_src
* Load profile with dconf
#+begin_src shell
dconf load /org/gnome/terminal/legacy/profiles:/:b1dcc9dd-5262-4d8d-a863-c897e6d979b9/ < $HOME/.dotfiles/terminal/profiles/ubuntu1.dconf
#+end_src
* Install Firefox
#+begin_src shell
sudo snap install firefox
#+end_src
* Install Doom Emacs
** Install
#+begin_src shell
git clone --depth 1 https://github.com/doomemacs/doomemacs $HOME/.config/emacs
PATH=$PATH:$HOME/.config/emacs/bin
doom install
#+end_src
** Sync
#+begin_src shell
doom sync
#+end_src
* Oh-my-zsh
#+begin_src shell
sudo apt install git-core zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
#+end_src
** Fonts powerline
#+begin_src shell
sudo apt install fonts-powerline
#+end_src
** Starship
#+begin_src shell
curl -sS https://starship.rs/install.sh | sh
#+end_src
** zsh-syntax-highlighting
#+begin_src shell
git clone https://github.com/zsh-users/zsh-syntax-highlighting $HOME/.oh-my-zsh/custom/plugins/zsh-syntax-highlighting
#+end_src
** zsh-autosuggestions
#+begin_src shell
git clone https://github.com/zsh-users/zsh-autosuggestions $HOME/.oh-my-zsh/custom/plugins/zsh-autosuggestions
#+end_src
** zsh-vi-mode
#+begin_src shell
git clone https://github.com/jeffreytse/zsh-vi-mode $ZSH_CUSTOM/plugins/zsh-vi-mode
#+end_src
* Vi
#+begin_src shell
sudo apt install vim
#+end_src
* Install stow
#+begin_src shell
sudo apt-get -y install stow
stow .
#+end_src
* Utilities
** Ripgrep
#+begin_src shell
sudo apt install ripgrep
#+end_src
** Markdown
#+begin_src shell
sudo apt-get install markdown
#+end_src
** wl-clipboard
#+begin_src shell
sudo apt-get install wl-clipboard
#+end_src
** gnome-screenshot
#+begin_src shell
sudo apt-get install gnome-screenshot
#+end_src
** Graphviz
#+begin_src shell
sudo apt-get install graphviz
#+end_src
** shfmt
#+begin_src shell
sudo snap install shfmt
#+end_src
** ShellCheck
#+begin_src shell
sudo apt install shellcheck
#+end_src
** fd-find
#+begin_src shell
sudo apt install fd-find
#+end_src
* C compiler
#+begin_src shell
sudo apt install gcc g++
#+end_src
* JavaScript
** Node.js
#+begin_src shell
sudo apt install nodejs
#+end_src
** npm
#+begin_src shell
sudo apt install npm
#+end_src
* Install pipx
#+begin_src shell
sudo apt install pipx
#+end_src
* Python
** Pyenv
#+begin_src shell
git clone https://github.com/pyenv/pyenv.git $HOME/.pyenv
pyenv install $(pyenv install --list | grep -v - | grep -v b | grep 3.11 | tail -n 1)
pyenv global $(pyenv install --list | grep -v - | grep -v b | grep 3.11 | tail -n 1)
#+end_src
** Python/pyenv requirements
#+begin_src shell
sudo apt install \
    build-essential \
    curl \
    libbz2-dev \
    libffi-dev \
    liblzma-dev \
    libncursesw5-dev \
    libreadline-dev \
    libsqlite3-dev \
    libssl-dev \
    libxml2-dev \
    libxmlsec1-dev \
    llvm \
    make \
    tk-dev \
    wget \
    xz-utils \
    zlib1g-dev
#+end_src
** Install Dev Requirements by default
#+begin_src shell
pip install $HOME/.dotfiles/python/requirements-dev.txt
#+end_src
* Install Lastpass cli
#+begin_src shell
sudo apt-get --no-install-recommends -yqq install \
  bash-completion \
  build-essential \
  cmake \
  libcurl4  \
  libcurl4-openssl-dev  \
  libssl-dev  \
  libxml2 \
  libxml2-dev  \
  libssl3 \
  pkg-config \
  ca-certificates \
  xclip

sudo apt-get lastpass-cli
git clone git@github.com:lastpass/lastpass-cli.git
git checkout 1.3.6
cd lastPass-cli
make
PATH=$PATH:$HOME/lastpass-cli/build/
#+end_src
* Installing qutebrowser
** Installing via virtualenv
#+begin_src shell
apt install qutebrowser
#+end_src
** gvim for configuration
#+begin_src shell
sudo apt install vim-gtk3
#+end_src
* Install mpv
#+begin_src shell
sudo apt install mpv
#+end_src
* Yewtube
#+begin_src shell
pip install yewtube
#+end_src
* Install qtile
#+begin_src shell

# Update system
sudo apt-get update

sudo apt-get install -y python3-cffi libpangocairo-1.0-0 --reinstall

pip3 install xcffib==0.12.1

pip install git+https://github.com/qtile/qtile

echo "[Desktop Entry]
Name=Qtile
Comment=Qtile Session
Exec=/home/kaypro/.pyenv/versions/3.11.5/bin/qtile start
Type=Application
Keywords=wm;tiling" | sudo tee /usr/share/xsessions/qtile.desktop
#+end_src
* Installing awesome
#+begin_src shell
sudo apt update
sudo apt install awesome
#+end_src
* Install rust
#+begin_src shell
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
#+end_src
* Alacritty
#+begin_src shell
git clone https://github.com/alacritty/alacritty.git
apt install cmake pkg-config libfreetype6-dev libfontconfig1-dev libxcb-xfixes0-dev libxkbcommon-dev python3
cargo build --release
sudo tic -xe alacritty,alacritty-direct extra/alacritty.info
infocmp alacritty
# setup desktop entry (inside alacritty/)
sudo cp target/release/alacritty /usr/local/bin # or anywhere else in $PATH
sudo cp extra/logo/alacritty-term.svg /usr/share/pixmaps/Alacritty.svg
sudo desktop-file-install extra/linux/Alacritty.desktop
sudo update-desktop-database
# install manuals
# requires gzip and scdoc
sudo apt install scdoc
sudo apt install gzip
sudo mkdir -p /usr/local/share/man/man1
sudo mkdir -p /usr/local/share/man/man5
scdoc < extra/man/alacritty.1.scd | gzip -c | sudo tee /usr/local/share/man/man1/alacritty.1.gz > /dev/null
scdoc < extra/man/alacritty-msg.1.scd | gzip -c | sudo tee /usr/local/share/man/man1/alacritty-msg.1.gz > /dev/null
scdoc < extra/man/alacritty.5.scd | gzip -c | sudo tee /usr/local/share/man/man5/alacritty.5.gz > /dev/null
scdoc < extra/man/alacritty-bindings.5.scd | gzip -c | sudo tee /usr/local/share/man/man5/alacritty-bindings.5.gz > /dev/null


#+end_src

* Gogh-Go
** Easy install
#+begin_src shell
sudo apt-get install dconf-cli uuid-runtime
bash -c "$(wget -qO- https://git.io/vQgMr)"
#+end_src
** Non-interactive
#+begin_src shell
# clone the repo into "$HOME/src/gogh"
git clone https://github.com/Gogh-Co/Gogh.git gogh
cd gogh

# necessary in the Alacritty terminal
pip install -r requirements.txt
export TERMINAL=alacritty

# Enter theme installs dir
cd installs

# install themes
./atom.sh
./dracula.sh
#+end_src
** Install oceanic-next
* Install JRE
was already install
#+begin_src shell
sudo apt install default-jre
#+end_src
* Alsa
#+begin_src shell
sudo apt install alsa-utils
sudo apt-get install mediainfo
#+end_src

* Install emacs
- follow mastering emacs instructions on installing from source and with native/json/treesitter support
* Install sqlite
#+begin_src shell
sudo apt install sqlite3
#+end_src
* Install tmux
#+begin_src shell
sudo apt install tmux
#+end_src
* Install fzf
#+begin_src shell
git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf
~/.fzf/install
#+end_src
* Install neovim
- from source
#+begin_src shell
git clone https://github.com/neovim/neovim
cd neovim &&
git checkout stable
make CMAKE_BUILD_TYPE=RelWithDebInfo

git clone --depth 1 https://github.com/wbthomason/packer.nvim\
 ~/.local/share/nvim/site/pack/packer/start/packer.nvim
#+end_src
* Installed rust
* Install isync
#+begin_src shell
sudo apt install isync
#+end_src
** Email setup
* Discord
Follow [[https://itsfoss.com/install-discord-linux/]] point 3
Installed under /opt
* Matrix
#+begin_src shell
sudo apt install -y wget apt-transport-https

sudo wget -O /usr/share/keyrings/element-io-archive-keyring.gpg https://packages.element.io/debian/element-io-archive-keyring.gpg

echo "deb [signed-by=/usr/share/keyrings/element-io-archive-keyring.gpg] https://packages.element.io/debian/ default main" | sudo tee /etc/apt/sources.list.d/element-io.list

sudo apt update

sudo apt install element-desktop
#+end_src
** Setup element
look config.doom
